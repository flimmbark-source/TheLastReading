// Adventure action nodes. A card owns exactly one node; the graph supplies all
// related interpretations. Graph order is also the global, deterministic tie
// breaker when two Event approaches are equally close.

export const ACTION_NODES = Object.freeze({
  PHYSICAL: 'physical',
  AGGRESSION: 'aggression',
  PROTECTION: 'protection',
  ENDURANCE: 'endurance',
  COMPASSION: 'compassion',
  AUTHORITY: 'authority',
  MYSTERY: 'mystery',
  DECEPTION: 'deception',
  INVESTIGATION: 'investigation',
  TRANSFORMATION: 'transformation',
  CREATION: 'creation',
  FORTUNE: 'fortune',
});

export const NODE_GRAPH = Object.freeze({
  [ACTION_NODES.PHYSICAL]: Object.freeze([
    ACTION_NODES.AGGRESSION,
    ACTION_NODES.PROTECTION,
    ACTION_NODES.ENDURANCE,
  ]),
  [ACTION_NODES.AGGRESSION]: Object.freeze([
    ACTION_NODES.PHYSICAL,
    ACTION_NODES.DECEPTION,
    ACTION_NODES.AUTHORITY,
    ACTION_NODES.TRANSFORMATION,
  ]),
  [ACTION_NODES.PROTECTION]: Object.freeze([
    ACTION_NODES.PHYSICAL,
    ACTION_NODES.COMPASSION,
    ACTION_NODES.AUTHORITY,
    ACTION_NODES.ENDURANCE,
  ]),
  [ACTION_NODES.ENDURANCE]: Object.freeze([
    ACTION_NODES.PHYSICAL,
    ACTION_NODES.PROTECTION,
    ACTION_NODES.INVESTIGATION,
    ACTION_NODES.TRANSFORMATION,
  ]),
  [ACTION_NODES.COMPASSION]: Object.freeze([
    ACTION_NODES.PROTECTION,
    ACTION_NODES.CREATION,
    ACTION_NODES.MYSTERY,
    ACTION_NODES.AUTHORITY,
  ]),
  [ACTION_NODES.AUTHORITY]: Object.freeze([
    ACTION_NODES.PROTECTION,
    ACTION_NODES.AGGRESSION,
    ACTION_NODES.INVESTIGATION,
    ACTION_NODES.CREATION,
  ]),
  [ACTION_NODES.MYSTERY]: Object.freeze([
    ACTION_NODES.INVESTIGATION,
    ACTION_NODES.DECEPTION,
    ACTION_NODES.COMPASSION,
    ACTION_NODES.FORTUNE,
    ACTION_NODES.TRANSFORMATION,
  ]),
  [ACTION_NODES.DECEPTION]: Object.freeze([
    ACTION_NODES.MYSTERY,
    ACTION_NODES.AGGRESSION,
    ACTION_NODES.INVESTIGATION,
    ACTION_NODES.FORTUNE,
  ]),
  [ACTION_NODES.INVESTIGATION]: Object.freeze([
    ACTION_NODES.MYSTERY,
    ACTION_NODES.AUTHORITY,
    ACTION_NODES.DECEPTION,
    ACTION_NODES.ENDURANCE,
  ]),
  [ACTION_NODES.TRANSFORMATION]: Object.freeze([
    ACTION_NODES.ENDURANCE,
    ACTION_NODES.MYSTERY,
    ACTION_NODES.AGGRESSION,
    ACTION_NODES.CREATION,
    ACTION_NODES.FORTUNE,
  ]),
  [ACTION_NODES.CREATION]: Object.freeze([
    ACTION_NODES.COMPASSION,
    ACTION_NODES.AUTHORITY,
    ACTION_NODES.TRANSFORMATION,
    ACTION_NODES.FORTUNE,
  ]),
  [ACTION_NODES.FORTUNE]: Object.freeze([
    ACTION_NODES.MYSTERY,
    ACTION_NODES.DECEPTION,
    ACTION_NODES.TRANSFORMATION,
    ACTION_NODES.CREATION,
  ]),
});

export const ACTION_NODE_LIST = Object.freeze(Object.values(ACTION_NODES));
